import numpy as np
import math 
import PIL
from PIL import Image
from scipy.interpolate import griddata
from scipy import interpolate
from functions import *
mycolor = 150
im = PIL.Image.new(mode = "RGB", size = (200, 200), color = (mycolor, mycolor, mycolor))
im.show()
